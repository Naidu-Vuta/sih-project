# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# Default zero-setup local SQLite database:
DATABASE_URL="file:./dev.db"

# For PostgreSQL (e.g. docker compose up -d or local server):
# DATABASE_URL="postgresql://postgres:postgrespassword@localhost:5432/coop_gig_db?schema=public"

# Authentication Security
JWT_SECRET="cooperative_super_secret_jwt_key_sih_2026"
JWT_EXPIRES_IN="7d"

# Client CORS URL
FRONTEND_URL="http://localhost:5173"
